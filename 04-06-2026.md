# 04-06-2026: VIETSHOP - Báo cáo & Kế hoạch chi tiết

## I. CÔNG VIỆC ĐÃ HOÀN THÀNH HÔM NAY

### Phase 1: UI Components Cốt Lõi (Template Matching)

| # | File | Task | Chi tiết |
|---|------|------|----------|
| 1 | `frontend/src/app/(public)/page.tsx` | `aspect-[3/4]` | ProductCard: `aspect-square` → `aspect-[3/4]` (ảnh dọc như template) |
| 2 | `frontend/src/app/(public)/page.tsx` | Horizontal scroller | "Hàng giá rẻ": đổi từ `grid` → `overflow-x-auto hide-scrollbar`, card `min-w-[280px]` |
| 3 | `frontend/src/app/(public)/page.tsx` | `lg:grid-cols-5` | Grid gợi ý thêm `lg:grid-cols-5`, slice 5 thay 4 |
| 4 | `frontend/src/components/public/PublicNavbar.tsx` | Theme tokens navbar | `bg-[#0F172A]` → `bg-secondary`, `max-w-[1280px]` → `max-w-container-max`, `h-20` → `py-base`, `text-white/*` → `text-on-secondary/*` |
| 5 | `frontend/src/app/(public)/products/page.tsx` | `aspect-[3/4]` Product List | Product card image: `aspect-square` → `aspect-[3/4]` |
| 6 | `frontend/src/components/public/PublicFooter.tsx` | Container bottom bar | Thêm `max-w-container-max mx-auto px-margin-desktop` cho bottom bar |

### Phase 2: Tính Năng & UI Bị Thiếu (Template Matching)

| # | File | Task | Chi tiết |
|---|------|------|----------|
| 7 | `frontend/src/app/(public)/products/page.tsx` | Badge giảm giá % | `getBadge()`: tính % từ `purchase_price` vs `sale_price`, hiển thị `-X%` badge accent |
| 8 | `frontend/src/app/(public)/products/page.tsx` | Sort "Bán chạy nhất" | Thêm option `best-selling` vào dropdown sort |
| 9 | `frontend/src/app/(public)/products/[id]/page.tsx` | Customer Reviews tab | Tab thứ 3 với rating tổng `4.8`, 3 review mẫu |
| 10 | `frontend/src/app/(public)/products/[id]/page.tsx` | Feature cards | Icon `battery_charging_full` + `noise_aware` trong Description tab |
| 11 | `frontend/src/app/(public)/page.tsx` | Review count text | Thêm `4.8 (120)` bên cạnh 5 sao trên ProductCard |
| 12 | `frontend/src/components/public/PublicNavbar.tsx` | Login minimal mode | Detect `/login`: ẩn nav/search/cart, hiển thị nút "Đăng nhập", `bg-on-background` |
| 13 | `frontend/src/components/public/PublicFooter.tsx` | Login minimal footer | Nếu `/login`: footer đơn giản chỉ copyright + 4 policy links, `bg-on-background` |

### Phase 3: Tinh Chỉnh (Template Matching)

| # | File | Task | Chi tiết |
|---|------|------|----------|
| 14 | `backend/apps/products/filters.py` | Category filter BE | Đổi `UUIDFilter` → `CharFilter(method)`, split comma, dùng `category_id__in` hỗ trợ multi-select |
| 15 | `frontend/src/app/(public)/products/page.tsx` | Category filter FE | `selectedCat` (string) → `selectedCats` (string[]), radio → checkbox, API gửi `.join(",")` |
| 16 | `frontend/src/app/(public)/login/page.tsx` | `rounded-xl` | `rounded-2xl` → `rounded-xl` |
| 17 | `frontend/src/app/(public)/products/page.tsx` | `rounded-xl` | Sidebar + product card: `rounded-2xl` → `rounded-xl` |
| 18 | `frontend/src/app/(public)/page.tsx` | `rounded-xl` | ProductCard, hàng thanh lý, hàng giá rẻ, blog images |
| 19 | `frontend/src/app/(public)/blogs/page.tsx` | `rounded-xl` | Featured article + blog cards |
| 20 | `frontend/src/app/(public)/about/page.tsx` | `rounded-xl` | Contact info card + form card |
| 21 | `frontend/src/app/(public)/products/[id]/page.tsx` | `rounded-xl` | Main image container + related product card |
| 22 | `frontend/src/app/(public)/cart/page.tsx` | `rounded-xl` | Cart item + summary card |

### Bug Fixes

| # | File | Bug | Fix |
|---|------|-----|-----|
| 23 | `backend/core/permissions.py` | `IsManager` dead code | Xoá class `IsManager` (không dùng) |
| 24 | `frontend/src/app/(public)/page.tsx` | `sale_price=0` nhập nhằng | Đồng bộ: `null` = "Liên hệ", `0` = "Miễn phí", `>0` = formatCurrency |
| 25 | `frontend/src/app/(public)/products/page.tsx` | `sale_price=0` nhập nhằng | Đồng bộ logic + discount check |
| 26 | `frontend/src/app/(public)/products/[id]/page.tsx` | `sale_price=0` nhập nhằng | Đồng bộ logic detail + related |
| 27 | `backend/apps/blogs/urls.py` + `views.py` | Blog URL UUID vs slug | Một route `<slug:slug>` duy nhất, `get_object()` thử UUID trước → fallback slug |

---

## II. KẾ HOẠCH CÔNG VIỆC CÒN LẠI

### 🟢 Ưu tiên 1: Cart & Payment (GIỎ HÀNG & THANH TOÁN)

**Hiện trạng:** Cart client-side (localStorage), nút "Tiến hành đặt hàng" chưa có action. Chưa có backend order/checkout/payment.

#### Phase A: Backend - Order System

| # | Task | File | Mô tả |
|---|------|------|-------|
| 1 | Tạo app `orders` | `backend/apps/orders/` | App mới: models, views, serializers, urls |
| 2 | Model `Order` | `orders/models.py` | `user` (FK, nullable), `status` (pending/confirmed/shipped/delivered/cancelled), `subtotal`, `shipping_fee`, `total`, `shipping_name`, `shipping_phone`, `shipping_address`, `note`, `payment_method`, `payment_status`, `created_at`, `updated_at` |
| 3 | Model `OrderItem` | `orders/models.py` | `order` (FK), `product` (FK), `product_name`, `product_image`, `quantity`, `unit_price`, `subtotal` |
| 4 | Serializer `OrderSerializer` | `orders/serializers.py` | Create: nhận items từ cart, tự tính subtotal. List: trả về items + total |
| 5 | ViewSet `OrderViewSet` | `orders/views.py` | `GET /api/orders/` (user's orders), `POST /api/orders/` (create), `GET /api/orders/:id/` |
| 6 | URLs | `orders/urls.py` | `/api/orders/`, `/api/orders/<uuid:pk>/` |
| 7 | Register app + migrate | `core/settings/base.py` | Thêm `orders` vào `INSTALLED_APPS` |

#### Phase B: Backend - Payment Gateway (VNPay)

| # | Task | File | Mô tả |
|---|------|------|-------|
| 8 | Model `Payment` | `orders/models.py` | `order` (FK), `amount`, `method` (COD/VNPay), `transaction_id`, `status`, `paid_at` |
| 9 | VNPay helper | `orders/payment.py` | `create_payment_url()`, `verify_return()` |
| 10 | Payment API | `orders/views.py` | `POST /api/payment/init` → URL VNPay, `GET /api/payment/return` → verify |
| 11 | Signal: Order confirmed | `orders/signals.py` | Tự động cập nhật product status |

#### Phase C: Frontend - Checkout Flow

| # | Task | File | Mô tả |
|---|------|------|-------|
| 12 | Service `orders.ts` | `frontend/src/services/orders.ts` | `createOrder()`, `getOrders()`, `getOrder()` |
| 13 | Service `payment.ts` | `frontend/src/services/payment.ts` | `initPayment()` |
| 14 | Checkout page | `(public)/checkout/page.tsx` | Form: shipping info, chọn payment method, order summary |
| 15 | Kết nối Cart → Checkout | `cart/page.tsx` | Nút "Tiến hành đặt hàng" → `/checkout` |
| 16 | Order confirmation | `(public)/order/[id]/page.tsx` | Trang cảm ơn sau đặt hàng thành công |
| 17 | Order history | `(public)/account/orders/page.tsx` | Danh sách đơn hàng |
| 18 | Order detail | `(public)/account/orders/[id]/page.tsx` | Chi tiết + trạng thái |

#### Phase D: Frontend - Admin Order Management

| # | Task | File | Mô tả |
|---|------|------|-------|
| 19 | Admin Orders page | `admin/orders/page.tsx` | DataTable + filter status |
| 20 | Admin Order detail | `admin/orders/[id]/page.tsx` | Xem items, cập nhật trạng thái |

---

### 🟡 Ưu tiên 2: Testing

**Hiện trạng:** Chưa có bất kỳ test nào (cả BE lẫn FE).

| # | Task | File | Mô tả |
|---|------|------|-------|
| 21 | Backend: setup pytest | `pytest.ini`, `requirements.txt` | Thêm `pytest`, `pytest-django`, `pytest-cov` |
| 22 | BE: Model tests | `* /tests/test_models.py` | Test User, Product, Purchase, Sale models |
| 23 | BE: API tests | `* /tests/test_api.py` | Test CRUD endpoints, permissions, filters |
| 24 | Frontend: setup vitest | `vitest.config.ts`, `package.json` | Thêm `vitest`, `@testing-library/react`, script `test` |
| 25 | FE: Component tests | `*.test.tsx` | Test ProductCard, Cart, Login, Navbar |

---

### 🔵 Ưu tiên 3: Deployment & Polish

| # | Task | Mô tả |
|---|------|-------|
| 26 | Docker Compose test | Chạy thử full stack Docker, fix lỗi nếu có |
| 27 | Nginx SSL | Cấu hình HTTPS với Let's Encrypt |
| 28 | Remove `.env` từ tracking | Kiểm tra `.env` không bị commit |

---

## III. TỔNG QUAN TRẠNG THÁI DỰ ÁN

| Hạng mục | Trạng thái |
|----------|-----------|
| Template Matching (3 Phases) | ✅ Hoàn thành (22 tasks) |
| Bug Fixes | ✅ Hoàn thành (5 bugs) |
| Cart & Payment | ⏳ Chưa bắt đầu |
| Testing | ⏳ Chưa bắt đầu |
| Deployment | ⏳ Chưa kiểm tra |
