# 02-06-2026 — Tổng kết công việc

## 1. Fix 500 lỗi Slider + Blog edit page
**File:** `apps/common/admin_mixins.py`

**Before:** `__init__` truy cập `self.fields[self.public_id_field]` khi field đó là readonly → **KeyError 500**

**After:** Kiểm tra `field in self.fields` trước khi set widget.

## 2. Preview ảnh trước khi save + Crop ảnh
**File:** `apps/common/admin_mixins.py` (viết lại hoàn toàn)
**File mới:** `apps/common/static/admin/js/croppable_image.js`

Tích hợp **Cropper.js** — khi chọn file:
- JS đọc file bằng `FileReader` → hiển thị trong Cropper.js
- Dropdown tỉ lệ: **16:9, 4:3, 1:1, Free**
- Kéo crop → tọa độ lưu vào hidden fields

**Khi save:**
1. Pillow crop ảnh theo tọa độ
2. Resize về max 1920px (giữ tỉ lệ)
3. Tối ưu JPEG quality 85
4. Upload lên Cloudinary

## 3. Xoá ảnh (dùng text thay thế)
Checkbox **"Xóa ảnh hiện tại (dùng text thay thế)"**:
- Tick + save → `cloudinary.uploader.destroy(public_id)` + clear URL

## 4. Settings: site_logo_url không còn hiện lộn xộn
`get_fieldsets` tự động remove `url_field` khỏi fieldsets → không còn label thừa.

## 5. Chỉ 1 SiteSettings record + chặn tạo mới
- Xoá record thừa khỏi DB
- Thêm `has_add_permission` → singleton

## 6. JS không load (404) → thêm collectstatic
Thêm `collectstatic --noinput` vào docker-compose startup command.

## 7. seed_demo crash khi restart
Sửa blog post creation dùng `slug` thay vì `title` để check trùng, tránh duplicate key error.

## 8. Slider model: cho phép blank URL
`image_url = models.URLField(max_length=500, blank=True)` — cho phép xoá ảnh.

---

## Files bị ảnh hưởng

| File | Thay đổi |
|---|---|
| `apps/common/admin_mixins.py` | Viết lại (247 dòng) |
| `apps/common/static/admin/js/croppable_image.js` | Mới |
| `apps/settings/admin.py` | Sửa |
| `apps/sliders/models.py` | Sửa (`blank=True`) |
| `core/settings/base.py` | Sửa (thêm `apps.common`) |
| `docker-compose.yml` | Sửa (thêm `collectstatic`) |
| `scripts/seed_demo.py` | Sửa (blog dùng slug) |

## Test

| Page | Status |
|---|---|
| Slider list + change | 200 ✅ |
| Blog list + change | 200 ✅ |
| Settings list + change | 200 ✅ |
| Add new Settings | 403 (bị chặn) ✅ |
| `/static/admin/js/croppable_image.js` | 200 ✅ |
